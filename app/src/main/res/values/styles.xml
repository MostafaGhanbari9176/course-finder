<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dark_eq</item>
        <item name="android:textColor">@color/dark_eq</item>
        <item name="android:textSize">@dimen/txt_size_smal</item>
        <item name="android:textColorHint">@color/yellow_ios</item>
        <item name="colorPrimaryDark">@color/dark_eq</item>
        <item name="colorAccent">@color/yellow_ios</item>


    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
